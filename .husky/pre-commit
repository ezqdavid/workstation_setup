#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# Python-style repo gate
if command -v pre-commit >/dev/null 2>&1; then
  pre-commit run -a
fi

# Secrets protection
if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact
fi
